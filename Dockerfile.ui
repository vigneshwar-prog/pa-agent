FROM python:3.12-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir streamlit requests python-dotenv

COPY src/ui.py ./src/ui.py

EXPOSE 8501
CMD ["streamlit", "run", "src/ui.py", "--server.address", "0.0.0.0", "--server.port", "8501"]
